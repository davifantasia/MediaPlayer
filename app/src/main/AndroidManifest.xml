<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.davifantasia.mediaplayer" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FixedAspectRatioActivity"
            android:label="@string/title_activity_fixed_aspect_ratio"
            android:launchMode="singleInstance"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".AdaptiveAspectRatioActivity"
            android:label="@string/title_activity_adaptive_aspect_ratio"
            android:launchMode="singleInstance"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".FixedAspectRatioInstagramActivity"
            android:label="@string/title_activity_fixed_aspect_ratio_instagram"
            android:launchMode="singleInstance"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".LiveStreamActivity"
            android:label="@string/title_activity_live_stream" >
        </activity>
    </application>

</manifest>
